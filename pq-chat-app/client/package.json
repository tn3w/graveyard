{
  "name": "chat-client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "wasm:build": "wasm-pack build ../client-wasm --target web --out-dir ../client/src/wasm"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^27.4.0",
    "vite": "^6.0.0",
    "vitest": "^4.0.17"
  }
}
